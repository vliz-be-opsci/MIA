import { Readable, type ReadableOptions } from 'readable-stream';
declare class ReadableFromWeb<T> extends Readable {
    private readonly reader;
    private readerClosed;
    constructor(stream: ReadableStream<T>, options?: ReadableOptions);
    _read(): void;
    destroy(error?: Error): this;
}
declare function readableFromWeb<T>(stream: ReadableStream<T>, options?: ReadableOptions): Readable;
export { ReadableFromWeb, readableFromWeb };
