import type * as RDF from '@rdfjs/types';
import { AggregatorComponent } from './Aggregator';
export declare class GroupConcat extends AggregatorComponent {
    private state;
    private lastLanguageValid;
    private lastLanguage;
    static emptyValue(): RDF.Term;
    put(term: RDF.Term): void;
    result(): RDF.Term;
}
