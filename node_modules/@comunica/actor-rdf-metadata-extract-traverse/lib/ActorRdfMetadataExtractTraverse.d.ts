import type { MediatorExtractLinks } from '@comunica/bus-extract-links';
import type { IActionRdfMetadataExtract, IActorRdfMetadataExtractOutput } from '@comunica/bus-rdf-metadata-extract';
import { ActorRdfMetadataExtract } from '@comunica/bus-rdf-metadata-extract';
import type { IActorArgs, IActorTest } from '@comunica/core';
/**
 * A comunica Traverse RDF Metadata Extract Actor.
 */
export declare class ActorRdfMetadataExtractTraverse extends ActorRdfMetadataExtract {
    private readonly mediatorExtractLinks;
    constructor(args: IActorRdfMetadataExtractTraverseArgs);
    test(_action: IActionRdfMetadataExtract): Promise<IActorTest>;
    run(action: IActionRdfMetadataExtract): Promise<IActorRdfMetadataExtractOutput>;
}
export interface IActorRdfMetadataExtractTraverseArgs extends IActorArgs<IActionRdfMetadataExtract, IActorTest, IActorRdfMetadataExtractOutput> {
    /**
     * Mediator for extracting links for traversal.
     */
    mediatorExtractLinks: MediatorExtractLinks;
}
