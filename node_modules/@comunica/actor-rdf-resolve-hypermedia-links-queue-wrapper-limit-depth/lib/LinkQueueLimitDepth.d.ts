import type { ILinkQueue, ILink } from '@comunica/bus-rdf-resolve-hypermedia-links-queue';
import { LinkQueueWrapper } from '@comunica/bus-rdf-resolve-hypermedia-links-queue';
/**
 * A link queue that only allows the given number of links to be pushed into it.
 */
export declare class LinkQueueLimitDepth extends LinkQueueWrapper {
    private readonly limit;
    constructor(linkQueue: ILinkQueue, limit: number);
    push(link: ILink, parent: ILink): boolean;
}
export declare const KEY_METADATA_DEPTH = "actor-rdf-resolve-hypermedia-links-queue-wrapper-limit-depth:depth";
